# i3status configuration file.
# see "man i3status" for documentation.

# It is important that this file is edited as UTF-8.
# The following line should contain a sharp s:
# ß
# If the above line is not correctly displayed, fix your editor first!

general {
        colors = true
        interval = 5
}

#order += "ethernet _first_"
order += "wireless _first_"
order += "battery all"
# order += "disk /"
order += "memory"
order += "tztime local"
order += "volume master"


# swaymsg -t get_inputs | jq -r '.[] | select(.identifier == "1452:591:Keychron_K2") | .xkb_active_layout_name'

wireless _first_ {
        format_up = "⇆ %essid%quality"
        format_down = "⛔"
}

ethernet _first_ {
        format_up = "⇆ %ip (%speed)"
        format_down = "⛔"
}

battery all {
        format = "%status %percentage %remaining %emptytime"
        format_down = "No battery"

        status_chr = "⚡ CHR"

        status_bat = "🔋 BAT"

        status_unk = "? UNK"

        status_full = "☻"
        
        path = "/sys/class/power_supply/BAT%d/uevent"

        low_threshold = 10
}

disk "/" {
        format = "%avail"
}

load {
        format = "%1min"
}

memory {
        format = " %used|%available "
        threshold_degraded = "1G"
        format_degraded = "MEMORY < %available"
}

tztime local {
        format = "%Y-%m-%d %H:%M"
}

volume master {
        format = "♪: %volume"
        device = "default"
        mixer = "Master"
        mixer_idx = 0
}
